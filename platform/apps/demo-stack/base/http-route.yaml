apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: architect-lab
spec:
  parentRefs:
    - name: architect-lab
      sectionName: http
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api
        - path:
            type: Exact
            value: /health
      backendRefs:
        - name: api
          port: 3000
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: web
          port: 8080
